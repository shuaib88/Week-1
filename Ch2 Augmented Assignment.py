score = 50;
score += 20;
d = 2;
d *= 3 + 4;
number = 10;
number *= number;
print (number);
# *= is called an augmented assignment operator. you can create one for
# all operators except negation.
number = 2;
number -= 13;
print (number);
# When augmented assigned operator is used, first expression to right
# of equal sign is evaluated and stored, then old variable is applied
#to operator and evaluated value. left to right. result is stored in
#variable
