number = 3;
print (number);
number = 2 * number;
print (number);
number = number * number;
print (number);
number = number * number;
print (number);
#computer reads line by line and when sees = 1)evaluates expression to right
#2) stores in memory 3)makes the variable name take that value. This repeats line
#by line
